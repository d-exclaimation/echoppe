{
  "private": true,
  "name": "echoppe",
  "version": "0.1.0",
  "license": "Apache-2.0",
  "scripts": {
    "re": "rm -rvf node_modules && rm -rvf packages/*/node_modules && yarn"
  },
  "workspaces": [
    "packages/*"
  ]
}
